Reference: Sipser公开课

[TOC]

## Finite Automaton = Regular Expression

定义DFA=$(Q,\Sigma,\delta,q,F)$。

通过简单PowerSet证明NFA和DFA等价。

语言是自动机所有接受的字符串的集合。

同时定义正则语言$L$为某个DFA接受的语言。

通过证明union，concatenation，star可以构造对应的NFA

来说明：Regular Expr 包含于 FA。



同时定义GNFA（把transition每条边变成RE），

证明GNFA包含于RE

来说明：FA 包含于RE。

这就说明两者等价。

## Pumping Lemma

证明对任意正则语言$A$，对$\exist p,\forall s\in A,|s| \ge p$，那$s$就可以分成三段$xyz,y\ne \epsilon,|xy|\le p$，并且$xy^iz,\forall i\ge0$都在$A$内。

本质是证明有限图上一条足够长的路径总是有环。

这是证明一个语言非正则的工具。



## CFL and PDA

Context Free Language 是变量替换语言。

grammar 和 language 的关系就和自动机和语言的关系一样。

Context Free的地方在于只替换变量，而不在于变量的上下文。

我认为一些Formal的东西也很美，所以还是写一下：
$$
\text{CFG} = (V,\Sigma,R,S)
\\
S:start \ variable
\\
V: variable
\\
\Sigma: alphabet
\\
R:rules :V\to(V\cup \Sigma)^{*}
$$
显然对于CFG的分解过程可以写成一棵树，我们称之为parse tree。

对于每一个可分解字符串parse tree唯一的CFG，我们称之为not-ambiguious的，反之亦然。



PDA是带有栈作为外部存储的NFA。写成六元组$(Q,\Sigma,\Gamma,\delta,q,F)$

其中$\delta$变成了：$Q \times \Sigma_{\epsilon}\ \times \Gamma_{\epsilon}\to P(Q\times \Sigma_{\epsilon})$

这其中不仅状态转移有不确定性，连堆栈都有不确定性了，这显然比NFA强得多。

学计算理论就要既重视细节，也要会丢掉细节，比如我们知道检测栈是否为空是简单的，因为我们只需要通过开始的时候无条件加一个不会用的符号这样就好了。

但是我们也不能丢掉细节，NFA和DFA是等价的，比如PDA和带栈的DFA是不等价的（栈没有不确定性，能力下降了很多）。









tobesolve：

如果栈变成队列会怎么样？



## Problem Set 1

作业好难，没有答案。

### 0.1

意思应该是CFL $\cup$ RE = RE 吧，显然。

### 0.2

PDA iff CFG：CFG to PDA是容易的，PDA to CFG需要考虑PDA栈的转换，构造变量$A_{p,q}$从空栈-空栈的转换。

### 1

[parallel addition]

构造一个带进位的减法器就行了。

但是正则语言是可翻转的，也就是说$\forall w \in \text{RE} , w^{R}\in \text{RE},w^{R} = \{a^{R},a\in w\}$

可以通过正则语法来证明。

所以加法器也是可以的。



**所以说证明语言需要既从语法考虑，又要从自动机考虑。**

### 2

[regular closure under perfect shuffle]

可以构造一个反复横跳的NFA。

### 3

[sequential addition]

直接使用pumping lemma可以证明这不是RE

### 4

[small NFA for: {w| w contains an a which is exactly k places from the end]

NFA的构造是简单的。

### 5

[any DFA for above language must be large]

证明一个DFA的状态有限，需要用到这样一个性质：$xz \in L, yz \notin L \implies q(x) \ne q(y)$

这个性质是显然的。

我们要证明DFA有$n$个有限状态，则需要找到一个集合$|S| =n$，使得$\forall x,y\in S,x\ne y,\exists z=\phi(x,y), (xz\in L) + (yz \in L) = 1\bmod2 $

也就是说我们对每对$x,y$都要找到一个方法。



### 6

[very tricky CFL]

CFL可以union。所以要善于使用分类。

分完类之后发现$i\gt j ,i\lt j\lt 2i,2i\lt j$

$i>j$和$2i\lt j$是类似的，说前者。

我们先生成$i=j$，然后让$i$变大一点就行了。
$$
\text{S} \to \text{0S1|T}\\
\\

\text{T} \to \text{0T |} \epsilon 
$$
第二种情况其实介于$i=j$ 和$2i =j$之间，那很神奇，我们只需要构造$a(i=j) + b(2i=j) ,a\gt 0,b\gt 0$即可。



### 7

[if-then-else ambiguous grammar]

矛盾出现在else不知道从属于哪个if。

根据现代编程语言的经验，我们可以将else规定属于最近的if。

那不合法的情况就是if没有被匹配完，去匹配后面的else了。

把if匹配完的称作M,没匹配完的称作U,这里只考虑IF的话，则：
$$
\text{E} \to \text{U|M}\\
\text{M}\to \text{if condition then M else M}
\\
\text{U} \to \text{if condition then E | if condition then M else U}
$$
关键就是让else前面是M （U的第二个生成）。





### 8

[synchronizing sequence]

(寻找synchronizing words似乎是NP-hard的。

我们考虑一个synchronizing word为$w$

(自由幺群？)

则$|Qw| = 1$。

因为集合的大小是不增的，我们找到让这个集合减小的地方就行了。

那就是存在两个状态，作用之后一样。考虑$q_{1,0}\to q_{1,1} \dots,q_{2,0}\to q_{2,1}\dots$这两个序列，最多不一样$\binom k 2$次。

（注意因果是因为存在$w$ ,我们证明这个$w$是可以缩小的）。



## Turing Machine