Reference: Sipser公开课

## Finite Automaton = Regular Expression

定义DFA=$(Q,\Sigma,\delta,q,F)$。

通过简单PowerSet证明NFA和DFA等价。

语言是自动机所有接受的字符串的集合。

同时定义正则语言$L$为某个DFA接受的语言。

通过证明union，concatenation，star可以构造对应的NFA

来说明：Regular Expr 包含于 FA。



同时定义GNFA（把transition每条边变成RE），

证明GNFA包含于RE

来说明：FA 包含于RE。

这就说明两者等价。

## Pumping Lemma

证明对任意正则语言$A$，对$\exist p,\forall s\in A,|s| \ge p$，那$s$就可以分成三段$xyz,y\ne \epsilon,|xy|\le p$，并且$xy^iz,\forall i\ge0$都在$A$内。

本质是证明有限图上一条足够长的路径总是有环。

这是证明一个语言非正则的工具。



## CFL and PDA

Context Free Language 是变量替换语言。

grammar 和 language 的关系就和自动机和语言的关系一样。

Context Free的地方在于只替换变量，而不在于变量的上下文。

我认为一些Formal的东西也很美，所以还是写一下：
$$
\text{CFG} = (V,\Sigma,R,S)
\\
S:start \ variable
\\
V: variable
\\
\Sigma: alphabet
\\
R:rules :V\to(V\cup \Sigma)^{*}
$$
显然对于CFG的分解过程可以写成一棵树，我们称之为parse tree。

对于每一个可分解字符串parse tree唯一的CFG，我们称之为not-ambiguious的，反之亦然。



PDA是带有栈作为外部存储的NFA。写成六元组$(Q,\Sigma,\Gamma,\delta,q,F)$

其中$\delta$变成了：$Q \times \Sigma_{\epsilon}\ \times \Gamma_{\epsilon}\to P(Q\times \Sigma_{\epsilon})$

这其中不仅状态转移有不确定性，连堆栈都有不确定性了，这显然比NFA强得多。

学计算理论就要既重视细节，也要会丢掉细节，比如我们知道检测栈是否为空是简单的，因为我们只需要通过开始的时候无条件加一个不会用的符号这样就好了。

但是我们也不能丢掉细节，NFA和DFA是等价的，比如PDA和带栈的DFA是不等价的（栈没有不确定性，能力下降了很多）。



tobesolve：

如果栈变成队列会怎么样？

PDA iff CFG,(CFG to PDA是容易的)

