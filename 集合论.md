Reference：[集合论：从高中到大学-哔哩哔哩](https://b23.tv/BNeH2Qe)

这视频讲的太好了。

总体还是集合论的超级入门。

[TOC]

## 集合论的利弊

唯一与最大的优点：所有的问题都是集合问题，而集合问题是简单的。

缺点：数学语言的分化，如方程的解是$x=a$和方程的解集是$\{a\}$并没有区别，但是复杂了；讲不清楚。

## 三大主义

逻辑主义 Russell：一切数学归于纯粹逻辑。

形式主义 Hilbert：一切数学归于数学公理。

这两个东西和朴素集合论是类似的。被ZFC集合论补救。

但是还是被哥德尔打破。

于是有了结构主义：没有先验的数学，只有后验的数学。系统无法证明自己的一致性，所以总会有更高的一层。

视频中的一段话我觉得说的很好：**我们脚下是一块石头，尽管这块石头并不牢固，但是我们要的不是踢这块石头，而是在石头上面跳舞**。

哥德尔第一定理：（有自然数的）系统是不完备的。但是我们一般碰不到那些不完备的命题。 

哥德尔第二定理：（有自然数的）系统无法证明自身的一致性。但是目前没有人证明(ZF)集合论是不一致的。

所以结论是：当成集合论是完备的、一致的、先验的数学。可耻但是好用。



## 公理和模型论

我们既然认为集合论的统治地位，那就应该只有集合论的公理。

公理本就是形式主义的说法。我们结合形式主义和结构主义的观点：

1.形式主义：首先构造一系列对象，它们之间的关系和满足的公理，形成了一个公理系统。

2.结构主义：然后将对象解释为集合，关系和公理解释为这个集合满足的性质。

上述做法被称为模型论。

例子：

平面几何（Hilbert） 

对象：点、直线； 

关系：从属关系、介于关系、全等关系； 

公理：从属公理（3）、介于公理（4）、全等公理（6）、平行公理（1）、完备公理（1）；

嵌入集合论：点视为元素，直线视为集合，从属关系视为集合的属于，介于关系和全等关系是满足介于公理和全等公理的三元与二元关系。

还有比如Peano公理等理论也可以嵌入集合论。



## 数学的严谨性和艺术性

艺术性：例子是对微积分的早期运用。

严谨性：例子是对微积分正确性的进一步证明。

这两个东西都要有，但是在讨论严谨的数学的时候，根据结构主义，我们应该只讨论在讨论范围内的东西。

## 朴素集合论

> 我们设置栅栏，把羊群围住，免受狼的侵袭，但是很可能在围栅栏时就已经有一只狼被围在其中了。 

首先介绍集合论之前，我们要对基本逻辑有理解，数学的基础在于基本的逻辑，所以逻辑符号可以简单地用于数学的“起点”。

朴素集合论形式主义说法就是概括公理：

$P(x)$是一个命题，则$\{x|P(x)\}$是一个集合。

而这会引出Russell悖论：$\{x|x\notin x\}$是一个集合，记为$S$，那么$S\in S \iff S\notin S$，这是不合理的。

在一些理论中这里的$\{x|P(x)\}$被称为类：class。

于是我们要引入ZFC集合论。

## ZFC集合论

> 所有集合不构成一个集合。

Z: Zermelo，策梅洛。

F: Fraenkel，弗兰克尔，加入了替换公理。

C: Choice，选择公理。

集合论中可以看成有两个对象：集合、元素。

但是其实元素也可以理解成集合，元素只是一种针对集合内东西的说法。但本质上还是只有一种对象，集合。

从逻辑上的角度来说，我们这里的论域（domain）是所有集合。

所以下面的$\forall,\exists$等符号都是以所有集合作为论域的，一般来说$\forall X$的论域是所有集合，但也有情况如$\forall x\in X$缩写成$\forall x$，但这其实非常坏文明，所以在ZFC的部分都尽量避免了这样的写法。

ZFC有若干条公理。

### 外延公理

外延公理是为了说明集合什么时候相等。

集合论中唯一的对象就是集合，所以这其实给出了判断所有东西是否相等的判定方法。

定义的时候，我们肯定希望这里的相等符合自反、对称和传递（等价关系），下面也是符合这个要求的。
$$
X=Y \iff \forall x(x\in X \leftrightarrow x\in Y)
$$
应用：

1.可以推出来集合内元素的不重复性。

2.说明了列举法的正确性。

注意的是，我们每定义一些集合，我们其实都需要用外延公理判断是否是良定义的，即使大部分时候这是显然的。



### 分离公理

我们要避免掉罗素悖论，则我们需要一点限制来约束概括公理:

$X$是一个集合，$P(x)$是一个命题，则$\{x\in X|P(x)\}$是一个集合。

根据外延公理，这是一个良定义的符号。

应用：

1.可以看出分离公理说明了子集的存在性。记$X\subset Y\iff \forall x \ x\in X \leftrightarrow x\in Y$

2.空集的定义：$\emptyset_X = \{x\notin X|x\ne x\}$。这个定义依赖于$X$，但是可以用外延公理说明$\emptyset_X =\emptyset_Y$，所以可以记为$\emptyset$。（个人理解：某种意义上，空集是ZFC集合论里唯一的元素。）

3.根据分离公理：$\{x\in X|x\in Y\}$是一个集合，则可以定义交集$X\cap Y$，通过外延公理可以证明$X\cap Y = Y\cap X$。

4.同理可以构造差集:$X \backslash Y = \{x\in X|x\notin Y\}$

### 并集公理

既然分离公理定义了交集，我们自然想定义并集。

并集公理：$\forall X\exists Y\forall x(\exist Z(Z\in X \and x\in Z) \iff (x \in Y))$

即我们要考虑$Z$的并集，我们首先把它们放到一起构成一个集合$X$，然后把它们并起来。

这里得到的$Y$我们称为一些$Z$的并集。(根据外延公理良定)

值得注意的是，我们还并没有证明一些集合放在一起是一个集合，只是当集合可以放在一起成为一个集合，那么就可以做并。

### 配对公理

配对公理可以用于上述情况。

$\forall X\forall Y\exist Z (x=X \or x=Y)\iff x\in Z$

可以看到这里只考虑两个元素的集合$\{X,Y\}$。

这里我们就真的将两个集合放在一起了，真正定义了并集。

因为元素也是集合，所以可以理解成元素放在一起也变成集合。

推论：当$X=Y$，$\{X\}$是一个集合。

### 幂集公理

上面的公理并不能保证我们的集合论里有很多集合。可能只有一个空集上面的公理也是成立的。

我们总希望可以有很多很多的集合，幂集公理就可以帮我们创造任意有限个元素的集合。

幂集公理：$\forall X\exist Y\forall Z(Z\subset X \iff Z \in Y)$

这里得到的$Y$记为$P(X)$，根据外延公理良定。

比如说要创造一个有$4$个元素的集合，我们可以考虑空集的幂集的幂集。通过分离公理，可以创造任意有限多个的元素的集合。



### 无穷公理

任意有限$\ne$无限：我在学习了微积分的基础知识之后，总觉得无限是不存在的，无限可以化为任意有限。但是事实上这只是分析的结果。直觉主义者就认为没有无限，而只有任意有限，如果这样的话，自然数集就不是一个集合，所以一般来说，还是承认无限。

无穷公理：$\exist X (\emptyset \in X \and (x\in X \Rightarrow P(x)\in X))$

这样生成的集合是的元素个数是无限的，因为空集、空集的幂集、空集的幂集的幂集...都是不相等的。

事实上无穷公理生成了自然数集，把Peano公理嵌入集合论。

自然数集可以写成（无穷公理有很多等价的写法，可以看出下面的这个和上面写的这个不太一样）：
$$
0 = \emptyset
\\
1=\{0\} = \{\emptyset\}\\
2 = \{0,1\} = \{\emptyset,\{\emptyset\}\}\\
\dots
$$

### 正则公理

其实上面的内容，大部分和朴素集合论重合。

我们需要考虑的是怎么避免Russel悖论。回头看Russel悖论，Russel悖论只用到了这样3点：分离定律，排中律，所有集合构成一个集合。

分离定律不能修改，因为我们总是希望一个对象的子对象是对象，如果集合的子集不是一个集合，就会很难受。而排中律是逻辑学的结果，这个是在数学之前的东西。所以为了防止这一点，可以看出，就是要让所有集合构成的“集合”不是一个集合。

正则公理：$\forall X\exist Y (Y\in X)\and (X\cap Y = \emptyset)$

可以看出，上面的公理都是为了定义某些集合，而只有正则公理是限制集合的性质的，就是没有这么定义，不像公理，反而像一个定理。（如果要攻击ZFC集合论，那当然要从正则公理入手）。



正则公理的推论：

1.不存在$X=\{X\}$。

2.不存在$X\in X$。

3.不存在集合列:$X_{n+1} \in X_n$，$n$是自然数。

4.不存在$X = \{\{X\}\}$

证明：

1.显然$X \ne \emptyset$，根据正则公理，应该存在$Y\in X$使得$X \cap Y = \emptyset$，而$Y$只能是$X$，$X \cap X = X \ne \emptyset$，矛盾。

2.若$X\in X$由分离公理：$Y = \{y \in X| y= X\}$是集合，则$Y=\{X\}$，和上面同理$X \cap Y = \{X\} \ne \emptyset$，矛盾。

3.值得注意的是可以让$X_n \in X_{n+1}$，只需要让$X_{n+1}=P(X_n)$即可。

这里的证明要先承认无限多个集合放在一起也是一个集合（会在后面证明）。

令$Y = \{X_0,X_1,\dots\}$，假设$Z = X_k \in Y ,Z\cap Y = \emptyset$，而实际上起码有：$X_{k+1} \in Y\cap X_k$。

这条定理的直观意思是，我们不能有一个集合，里面的元素是一个集合，然后集合里面还有元素是一个集合...这样无限深挖下去。

4.注意到$X \in \{X\} \in X \in \{X\} \dots$，不符合3。

替代公理和选择公理将在后面介绍。

## 二元关系

笛卡尔积 Cartesian Product：由配对公理$x\in X,y\in Y,\{x,y\}$是一个集合。称为$x,y$的无序数对。

同样由配对公理：$\{\{x\}, \{x,y\}\}$也是一个集合，这个称为$x,y$的有序数对，记为$(x,y)$。

由幂集公理和并集公理：$P(P(X\cup Y))$是一个集合。

由分离公理$\{a \in P(P(X\cup Y))|\exist x\in X\exist y\in Y,a=(x,y) \}$是一个集合。

这个集合我们称为$X\times Y$，也就是$X$和$Y$的Cartesian Product。

这里用的很多东西都是技术上的，比如$\{\{x\}, \{x,y\}\}$和$\{x,\{x,y\}\}$也是差不多的。

不交并：$X \sqcup Y = \{X\times \{\emptyset\}\}\cup \{Y \times \{\{\emptyset\}\}\}$

二元关系：$R\subset X\times Y$。当$(x,y) \in R$，记为$xRy$。通常来说我们考虑一个集合上的二元关系，也就是$X=Y$。



### 等价关系

如果$R$为$X$上的二元关系，并且满足：

1.自反性$xRx$

2.对称性$xRy \iff yRx$

3.传递性$(xRy) \and (yRz) \Rightarrow xRz$

我们称为等价关系，用$\sim$表示。

若$\sim$为$R$上的一个等价关系，记$[x] = \{y\in X|x\sim y\}$为等价类。$x$称为$[x]$的一个代表元。显然$x\sim y \iff [x] = [y]$

$\{[x]\in P(X) | x\in X\}$被称为商集，记为$X/\sim$ 

例子：考虑$\mathbb R$上的等价关系$\sim:x-y \in \mathbb Z$。$\mathbb R \backslash \sim \cong  S^1$，是一个单位圆，这里涉及到代数中同构的概念。

不变量：若$f$是一个$X$的函数（函数的概念在后面会介绍），且$x \sim y \iff f(x) = f(y)$，则$f$是$\sim$下的不变量。如斜率是平行关系的不变量。

## 偏序关系

和离散数学中的内容基本一致。

若$X$上的二元关系$R$满足：

1.自反性$xRx$

2.反称性$(xRy \and yRx) \Rightarrow x=y$

3.传递性$(xRy \and yRz) \Rightarrow xRz$

则称为偏序关系。

1.2可以直接把2改成双箭头来代替。

我们把偏序关系$R$记为$\preceq$。$xRy$记为$x\le y$。

（感觉符号$\preceq$和$\le$并不需要区分。）

例子：自然数的整除关系。

全序关系：$\forall x\in X\forall y\in X (x\le y \or y\le x)$

例子：整数的小于等于关系。

极小元(minimal element):$\forall y\in X\ ( y\le x \Rightarrow x=y)$，则称$x$是极小元。极大元同理。

最小元(least element):$\forall y\in X(x\le y)$，则称$x$是最小元。最大元同理。

下界：$x\in X,Y\subset X,y\in Y \Rightarrow x\le y$，则$x$称为$Y$的下界。上界同理。

## 函数关系

若$R$为$X$和$Y$上的二元关系，$(xRy_1)\and(xRy_2) \Rightarrow y_1=y_2$，则称$R$是一个函数关系。

若$xfy$，我们记$f(x)=y$，由函数关系的定义可知记号是良定的。其中$X$称为$f$的定义域，$Y$称为$f$的陪域。

记为$f:X \to Y,x \mapsto y$

值域$\{y\in Y |\exist x\in X(f(x)=y)\}$记为$f(X)$，也称$f$的值域。

### 替代公理

在函数关系中，我们知道$Y$是一个集合，从而根据分离公理，值域也是一个集合。而替代公理是处理$Y$不是一个集合的情况的。

替代公理：$X$是一个集合，$P(x,y)$是一个命题，若$(P(x,y_1)\and P(x,y_2)) \Rightarrow y_1=y_2$,则$\{y|\exists xP(x,y)\}$也是一个集合。

事实上替代公理要求对于每个$x$，存在一个$yP(x,y)$。

若没有这个条件，则可以用替代公理证明分离公理：
$$
Q(x,y) = P(x) \and (x=y)\\
\{y|\exist x (Q(x,y))\} =\{y\in X |P(y)\}
$$

### 单射左逆

单射：$x_1fy \and x_2fy \Rightarrow x_1=x_2$。

满射：$\forall y\exist x(f(x)=y)$

双射：既是单射，又是双射。	

我们证明一个单射有左逆，也就是对函数关系$f:X\to Y$，$f$为单射，有函数关系$g:Y\to X$，$g$为满射，且$g \circ f = 1_X$，其中$1_X : X \to X,x \mapsto x$。

（其实函数符合运算也是一种关系的复合运算。）

我们只需要让$g$中$f(X)$的部分映射回$x$，其他随便就可以了。可以这样写：$g' : f(X) \to X ,y\mapsto x$其中$f(x)=y$。

### 满射右逆

如果我们仿照上面的证明，证明存在满射右逆：对函数关系$f:X\to Y$是满射，存在单射$g:Y \to X$，$f\circ g = 1_Y$。

证明的困难在于对于每个$y$,在$X_y=\{x\in X|f(x)=y\}$中选择一个$x$，让$y$映射到$x$。但是没有一个方法，让我们从这些集合中每个集合恰好选择一个$x$。特别是$X_y$可能会很大，我们就更难给出一个显式的表示。

### 选择公理

选择公理其实和满射右逆的存在性是等价的。

给定若干个相互不交的非空集合，存在着至少一个集合，它与每个非空集合的交恰好有一个公共元素。

> 选择公理处理的核心是“若干个相互不交的集合”，它的准确表述是对于集合$X$，考虑$X$的所有元素，它们任意两个的交为空集。这里的“若干个”是多少个？是非常非常多个，多到我们不知道能不能用准确的语言去描述它（可以学完后面集合的势之后再回过来看这个问题）。这里的“集合”是什么集合？是很任意的集合，是不带有特殊结构的集合。 很多人不喜欢选择公理，主要原因便是选择公理在很多情况下其实都是可以被“证明”的。例如“若干个”是有限个、 这些相互不交的集合上有一个良序、这些相互不交的集合都是群。所以在实际应用中我们很少会用到选择公理， 因为我们实际上研究的集合要么没有那么多，要么都带有一些特殊结构。

选择公理的等价命题：

1.满射右逆，如上述。

2.选择函数存在：$X$是一个集合，存在函数$f:X\to \bigcup_{Y\in X}Y，$满足$f(S) \in S$。

3.笛卡尔积非空：$X_i$是若干个集合，则它们的笛卡尔积非空。

4.Zermelo良序定理：$X$是一个集合，存在$X$上的偏序关系$\preceq$,使得对于任意子集$Y \subset X$,$Y$中均存在极小元。

5.Hasudorff极大原理：$(X,\preceq)$是一个非空偏序集，存在全序子集$Y$,使得对于所有的$x\in X$，$\{x\}\cup Y$不是一个全序子集。

6.Zorn引理：$(X,\preceq)$是一个非空偏序集，如果$X$的全序子集都有上界，那么$X$中至少存在一个极大元。

数学证明很多时候也会尽量避开选择公理。在不得不使用的时候都会加上“这里用到了选择公理”来说明。

## 集合的势

不需要用到选择公理，双射有逆是显然的。

等势：$X,Y$是集合，如果存在一个双射$X\to Y$，则$X,Y$等势。

例子：

1.对于有限集而言：等势当且仅当元素数量相同。

2.一个集合和自己等势。

3.由对角线法，自然数集和自然数集的笛卡尔积等势。

证明：$f:\mathbb N^2 \to \mathbb N,(n_1,n_2)\mapsto (n_1+n_2+1)(n_1+n_2)/2-n_1$是一个双射。

等势是一个等价关系，因为双射复合还是双射。（但是关系要定义在集合上，所有的二元关系不一定是一个集合，所以需要在一些特定的集合上讨论）。

### 康托定理

Cantor定理：$X$和$P(X)$不等势。

证明：若存在双射$f:X \to P(X)$,考虑集合$T = \{x\in X|x\notin f(X)\}$，由双射，存在$S$使得$f(S) = T$。则有$S\in T \iff S\notin T$，这是矛盾的。

这个定理的证明有点像Russell悖论。

这个定理说明无穷集的势并不一样，对势需要更精细的刻画。

### 施罗德伯恩斯定理

定义：$X,Y$是集合，存在单射$X \to Y$，称为$X$的势小于等于$Y$的势，记为$|X| \le |Y|$或$card(X) \le card(Y)$。

因为等势是一个等价关系，我们自然会考虑，势是否构成一个偏序关系。

自反性和传递性都是显然的。单射符合之后仍是单射。

剩下就要证明反称性。这个命题等价于施罗德伯恩斯定理：

$X,Y$是集合，存在单射$f:X\to Y,g:Y\to X$，则存在双射$h:X\to Y$。

证明：（建议先看看下面的思想）

$X_0 =X,Y_0=g(Y)$，令$f': X\to X,x\mapsto g(f(x))$。设$f^{(n)}(x)$为$f'$的$n$次复合。设$X_n = f^{(n)}(X_0),Y_n = f^{(n)}(Y_0),Z_n = X_n \backslash Y_n$。

因为$f(X) \subset Y, g(Y) \subset X$

有$Y_0 \subset X_0,X_1 \subset Y_0,Y_1\subset X_1,X_2\subset Y_1$.

也就是：$X_{i+1} \subset Y_{i}\subset X_i$

可以看出$Z_n$之间是不交的，并且$Z_n = f^{(n)}(X_0)\backslash f^{(n)}(Y_0) = f^{(n)}(X_0\backslash Y_0)$。

设$Z = \sqcup Z_n$。

我们通过直接写出$h$来证明。

令$g':Y \to g(Y) ,y\mapsto g(y)$，则$g'$是一个双射。为了方便，下面直接用$g$表示$g'$。

当$x\in Z$，令$h(x) = f(x)$。当$x\notin Z$,让$h(x)=g^{-1}(x)$。

证明$h$是单射，一方面$f$和$g^{-1}$都是单射，另一方面，若$f(x_1) = g^{-1}(x_2)$,设$x_1 \in Z_n$,则$gf(x_1) = x_2$，则$x_2\in f'(Z_n) = Z_{n+1}$，矛盾。

证明$h$是满射，若$y \in g^{-1}(Z_n)$,则存在$x_0 \in Z_n,y=g^{-1}(x_0),Z_n = f^{(n)}(X_0 \backslash Y_0)$，而 $x_0 = gf(x),x\in Z_{n-1}$，$f(x) = g^{-1}(x_0) = y$。

否则，其他情况当$x=g(y)$，$h(x) = g^{-1}(y) = y$



上面这个证明的思想是这样的：

可以看成在一个偶图上考虑。则一个点只能在一条链上，链可以长这样：

1.有限的偶圈。

2.从$X$出发的无穷交错路

3.从$Y$出发的无穷交错路。

4.两端都无穷延申的无穷交错路。

偶圈是简单的，有两种可能的匹配。

两端都无穷延伸的，实际上也有两种可能的匹配。

只有从$X$出发的无穷交错路是一定要染$X$连的那条边，从$Y$出发的无穷交错路一定要染$Y$连的那条边。

那上面可以看成这样：

$X_n$是从$X$出发的走$2n$步的无穷交错路，$Y_n$是从$Y$出发的走$2n+1$步无穷交错路，$Z$就排除可以从$Y$出发的链上的点，也就是第1、3、4类点。

对于2类点，我们直接染色，对134类点，我们反过来染色。



证明集合的势是一个全序关系需要用到选择公理。

### 常见集合的势

若$X$为有限集，记集合$card(X)$为$X$的元素个数。

自然数集的势$card(N) = \aleph_0$,称为阿列夫$0$。与自然数集等势的集合成为可数集。

定理：自然数集是最小的无穷集。

证明：如果承认选择公理的话，那就是显然的。

定理：两个等势的集合的幂集等势。

证明：根据原来的双射构造新的双射。

定理：实数集$\mathbb R$和自然数集的幂集$P(\mathbb N)$等势。

证明：用二进制小数联系两者。

可以推出自然数集并上或除去有限集后与自然数集本身等势。

### 连续统猜想

定义：$\aleph_1$是大于$\aleph_0$的最小基数。同理可以定义更多。

连续统猜想：$\aleph_1 = 2^{\aleph_0}$。

### 指标集

定义：设$M$是一个集合，对于集合$I$，如果$M$和$I$等势，则称$I$是$M$的一个指标集，记为$M=\{M_i\}_{i\in I}$ 

## 附录

### 无限个集合的交

给出指标集之后，用替代公理，说明涉及到的集合放在一起还是集合。然后用分离公理。

### 无限个集合的并

和上面一样，只是使用的是并集公理。





