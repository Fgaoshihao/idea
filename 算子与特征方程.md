好的，我们将使用**更一般**的 $m$ 阶常系数线性齐次递推关系来严格证明，为什么当 $\alpha$ 是特征方程的一个重根时，$n^k \alpha^n$ 形式的解是成立的。



### 一般 $m$ 阶递推关系与特征方程



递推关系：



$$f(n) + a_1 f(n-1) + a_2 f(n-2) + \dots + a_m f(n-m) = 0$$



其中 $a_1, a_2, \dots, a_m$ 都是常数。

特征方程：



$$\alpha^m + a_1 \alpha^{m-1} + a_2 \alpha^{m-2} + \dots + a_m = 0$$

我们将特征方程的多项式记为 $P(\alpha)$：



$$P(\alpha) = \alpha^m + a_1 \alpha^{m-1} + a_2 \alpha^{m-2} + \dots + a_m$$



### 核心代数工具：后移算子 $E$



为了简洁地表示递推关系，我们引入后移算子 $E$：



$$E f(n) = f(n+1)$$

$$E^k f(n) = f(n+k)$$

将原递推关系中的 $n$ 替换为 $n+m$（使所有项的下标都为正数），得到：



$$f(n+m) + a_1 f(n+m-1) + a_2 f(n+m-2) + \dots + a_m f(n) = 0$$

使用算子 $E$，递推关系可以写成：



$$(E^m + a_1 E^{m-1} + a_2 E^{m-2} + \dots + a_m) f(n) = 0$$

注意：特征多项式 $P(\alpha)$ 和算子多项式 $P(E)$ 是形式一致的。



### $m$ 重根的条件



假设 $\alpha_0$ 是特征方程 $P(\alpha)=0$ 的一个 $m$ 重根。

根据代数定理，如果 $\alpha_0$ 是 $P(\alpha)=0$ 的 $m$ 重根，则 $\alpha_0$ 满足：



$$P(\alpha_0) = 0$$

$$P'(\alpha_0) = 0$$

$$P''(\alpha_0) = 0$$

$$\dots$$

$$\mathbf{P^{(k)}(\alpha_0) = 0} \quad \text{对于所有 } 0 \leq k \leq m-1$$



### 关键证明：使用微分操作



我们尝试证明 $f(n) = n^k \alpha_0^n$ 是递推关系的解，其中 $0 \leq k \leq m-1$。

**第一步：证明 $\alpha^n P(\alpha) = 0$ 恒成立**

对于任意 $\alpha$，我们有：



$$P(E) [\alpha^n] = (E^m + a_1 E^{m-1} + \dots + a_m) \alpha^n$$

$$P(E) [\alpha^n] = E^m \alpha^n + a_1 E^{m-1} \alpha^n + \dots + a_m \alpha^n$$

$$P(E) [\alpha^n] = \alpha^{n+m} + a_1 \alpha^{n+m-1} + \dots + a_m \alpha^n$$

提取公因式 $\alpha^n$:



$$P(E) [\alpha^n] = \alpha^n (\alpha^m + a_1 \alpha^{m-1} + \dots + a_m) = \alpha^n P(\alpha)$$

所以，我们得到一个重要的恒等式：



$$\mathbf{P(E) [\alpha^n] = \alpha^n P(\alpha)}$$

**第二步：对 $\alpha$ 求导 $k$ 次**

现在，我们对上方的恒等式两边对 $\alpha$ 求导 $k$ 次，其中 $0 \leq k \leq m-1$。

$$\frac{\partial^k}{\partial \alpha^k} \left[ P(E) [\alpha^n] \right] = \frac{\partial^k}{\partial \alpha^k} \left[ \alpha^n P(\alpha) \right]$$

处理左侧 (LHS)：

因为 $P(E)$ 中的算子 $E$ 只对 $n$ 作用，与 $\alpha$ 无关，所以微分和 $P(E)$ 算子可以交换顺序：



$$LHS = P(E) \left[ \frac{\partial^k}{\partial \alpha^k} \alpha^n \right]$$

我们知道 $\frac{\partial}{\partial \alpha} \alpha^n = n \alpha^{n-1}$，



$$\frac{\partial^k}{\partial \alpha^k} \alpha^n = n(n-1)\dots(n-k+1) \alpha^{n-k} = \mathbf{n^{\underline{k}} \alpha^{n-k}}$$



其中 $n^{\underline{k}}$ 是下降阶乘。

处理右侧 (RHS)：

我们对 $\alpha^n P(\alpha)$ 使用莱布尼茨乘积求导法则 $k$ 次：



$$\frac{\partial^k}{\partial \alpha^k} [\alpha^n P(\alpha)] = \sum_{j=0}^{k} \binom{k}{j} \left(\frac{\partial^j}{\partial \alpha^j} \alpha^n\right) \left(\frac{\partial^{k-j}}{\partial \alpha^{k-j}} P(\alpha)\right)$$

$$\mathbf{RHS = \sum_{j=0}^{k} \binom{k}{j} (n^{\underline{j}} \alpha^{n-j}) P^{(k-j)}(\alpha)}$$

**第三步：代入重根 $\alpha_0$**

现在，我们将重根 $\alpha_0$ 代入 $LHS = RHS$ 的等式中：

$$LHS = P(E) \left[ n^{\underline{k}} \alpha_0^{n-k} \right]$$

$$RHS = \sum_{j=0}^{k} \binom{k}{j} (n^{\underline{j}} \alpha_0^{n-j}) P^{(k-j)}(\alpha_0)$$

由于 $\alpha_0$ 是 $m$ 重根，且 $k \leq m-1$，则对于 $0 \leq k-j \leq k \leq m-1$，我们有：



$$P^{(k-j)}(\alpha_0) = 0$$

因此，RHS 中的每一项都是 $0$！



$$\mathbf{RHS} = 0$$

所以，我们得到：



$$P(E) \left[ n^{\underline{k}} \alpha_0^{n-k} \right] = 0$$

这证明了 $g(n) = n^{\underline{k}} \alpha_0^{n-k}$ 是递推关系的解。

**第四步：最终形式的修正**

我们将 $g(n)$ 乘以常数 $\alpha_0^k$（这不影响其作为齐次递推关系解的性质），并代入 $n^{\underline{k}} = n(n-1)\dots(n-k+1)$：



$$f(n) = \alpha_0^k \cdot g(n) = \alpha_0^k \cdot n^{\underline{k}} \alpha_0^{n-k} = n^{\underline{k}} \alpha_0^n$$

因为 $n^{\underline{k}} = n(n-1)\dots(n-k+1)$ 是一个关于 $n$ 的 $k$ 次多项式，可以表示为 $C_k n^k + C_{k-1} n^{k-1} + \dots + C_0$，而 $n^k \alpha_0^n$ 等这些形式都是 $n^{\underline{k}} \alpha_0^n$ 的线性组合。

因此， $f(n) = n^k \alpha_0^n$ （其中 $0 \leq k \leq m-1$）是解，可以作为一组线性无关解的基础。

**结论：**

**正是因为重根 $\alpha_0$ 导致了特征多项式及其低阶导数在 $\alpha_0$ 处同时为零 ($P(\alpha_0)=0, P'(\alpha_0)=0, \dots$)，才使得当 $n^k \alpha^n$ 形式代入递推关系时，其展开项能够抵消为零，从而满足递推关系。**

建议阅读具体数学、组合分析 