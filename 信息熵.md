Reference:[熵 (信息论) - 维基百科，自由的百科全书 (wikipedia.org)](https://zh.wikipedia.org/wiki/熵_(信息论)#定义)

## 熵是哪来的

熵是度量不确定性的性质。

而当一个几率较高的事情发生的时候，我们其实没有获取什么信息，而当一个几率较低的事情发生的时候，我们实际上获取了很多信息。

这就引入了一个信息量的概念。

若随机变量$X=x$，我们记得到的信息量为$I(x)$，根据我们上面的说法$I(x) = f(p(X=x))$，并且关于$p$是一个单调减的函数。

对于两个独立事件$A=a,B=b$，我们得到的信息量应该是可加的，因为这两个事件不相关。

所以有：
$$
I(A=a,B=b) = I(A=a) + I(B=b)\\
f(p(A=a)  P(B=b))=f(p(A=a,B=b)) =f(p(A=a))+f(p(B=b))
$$
显然我们可以通过构造一些事件，使得对于$\forall x,y,f(xy) = f(x)+f(y)$。

我们熟悉一个这样性质的函数，那就是对数函数。

忽略常数的话，一个单调减的对数函数可以为$f(x) = -\ln x$

那我们现在得到了：$I(X=x) = -\ln p(x)$



熵是不确定性的度量，一个随机变量携带的信息量越多，也就证明也越随机，也就是说熵也是一个随机变量期望信息量的度量。

所以我们定义熵为
$$
H(X) = k \cdot E[I(X)] = -k\sum_{x\in X} p(x)\ln p(x)
$$
$k$为一个调整对数函数的常数。一般可以忽略。



~~对连续随机变量，这个熵好像没什么意义，因为连续变量太不随机了。所以只考虑连续变量。~~

暂时只考虑离散变量。

## 条件熵

考虑两个随机变量$X,Y$。

我们希望知道在知道一个的情况下，还能从另外一个获取多少信息。

则定义$H(Y|X)$为知道$X$的情况下，对于$Y$还剩多少熵。

也就是定义为$H(Y|X) = E_X[H(Y|X=x)]$

则：
$$
H(Y|X) =E_X[H(Y|X=x)]\\
=\sum_{x\in X}p(x)H(Y|X=x)
\\
=-\sum_{x\in X} p(x) \sum_{y \in Y} p(y|x) \ln p(y|x)\\
=-\sum_{x\in X,y\in Y}p(x,y) \ln p(y|x)
$$


## 性质

### 对称性质

可以注意到$H$只和$X$的概率分布$p$有关。

对于离散变量的一些概率轮换是不影响熵的。

### 连续性

显然对于分布$p$，$H$非常的连续，也就是说略微更改概率，熵的改变不会很大。

这是符合直观理解的。

### 极值  

由Jensen不等式
$$
H(X) = E[\ln \frac{1} {p(X)}] \le \ln E[\frac{1} {p(X)}]
$$
当$X$只有$n$个取值，由均值不等式
$$
H(X) \le \ln E[n] \le \ln n
$$

### 条件

$$
H(Y|X) =-\sum_{x\in X,y\in Y}p(x,y) \ln p(y|x) \\= -\sum_{x\in X,y\in Y}p(x,y) (\ln p(x,y) - \ln p(x)) \\= H(Y,X) - H(X)
$$

也就是
$$
H(Y,X) = H(X) +H(Y|X) = H(Y) +H(X|Y)
$$
这告诉我们，对于两个信息，先知道哪一个是没有影响的。

若$y=f(x)$，则$H(Y|X)$为$0$，也是合理的。

若$X,Y$独立，则可以算出$H(Y|X) = H(Y)$，也是合理的。

## 微分熵

可以直接类似的得出
$$
H(X) = k \cdot E[I(X)] = -\int_x p(x) \ln p(x) dx
$$
对均匀分布$X -U(0,a)$
$$
H(X) = \int_0^a \frac 1 a \ln a dx = \ln a
$$
可以看出这里的熵并不非负了。

### 交叉熵

但我们编码的时候不一定知道真正的分布，如果我们错误地用$- \ln q(x)$的信息量编码出现概率为$p(x)$的时，我们编码的期望信息量可以看成：
$$
H(X) = -\sum_{x \in X} p(x) \ln q(x)
$$
举个例子，我们都用1个“字母位”去编码一个字母，也就是说我们默认$q(x) = \frac 1 {26}$，在26进制下，我们都用1位去编码字母，这时候的期望编码长度怎样都是$1$，但其实字母出现的概率是不同的，如果我们知道真正的$p(x)$，重新编码，就会使得编码长度更小。

所以交叉熵可以看成在错误分布$q$下，实际分布为$p$的期望编码长度。

定义相对熵为
$$
D_{KL}(p,q) = H(p,q) - H(p) = H(x) = -\sum_{x\in X}p(x)\ln \frac {q(x)}{p(x)} \ge 0
$$
注意到若$q(x) = p(x)$时，$D_{KL}$为$0$，我们可以把相对熵看成$q$到$p$的距离。

而$H(p,q) = D_{KL}(p,q) + H(p)$，所以交叉熵也反映了$p,q$的距离。



在机器学习模型中，我们并不知道$p$，我们用训练集其实就是我们的正确分布$p$，并且模型学习的结果就是我们的错误分布$q$。

比如对一个输入$x$，训练集的输出为$p(x)$，在分类问题中，$p(x)$是一个one-hot的东西。

而$q(x)$就是机器学习完之后softmax的结果。

对于这个$x$，交叉熵就为$H(p,q)$

对于整个训练集的损失函数，我们可以取所有训练集输入交叉熵的平均。



## 例子：ID3

看了很久也没看懂，到底要对什么取熵，这真的很困难。

我们分类的时候只是希望分完的类尽量简单。

是什么意思呢，我们其实只在乎样本的类作为一个随机变量的熵。

也就是说设样本$S=\{s_i\},s_i = (x_i,y_i)$，我们只关心$y_i$构成的随机变量的$Y$熵。

对一个属性$x_{i,k}$分类，就把$S$划分成 $S_1 ,S_2\dots$。我们需要的是这些熵尽量的小。

那$H(Y) = \sum_{Y} -p(Y=y)\ln p(Y=y)$

分完类就是$H(Y|A) = \sum_{Y} -p(Y=y|A=a) \ln p(Y=y |A =a)$

找到$A$使$H(Y,A)$最小就行了。

