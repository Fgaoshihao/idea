Reference: 

[算术平均和几何平均之间存在别的平均吗](https://www.bilibili.com/video/BV1GM4m1k7Kn?vd_source=283024c2d484190cd3fedf6a07cde95d)

[高斯也有计算错误？与他的发现相比实在微不足道](https://www.bilibili.com/video/BV1g94y1w7sy?vd_source=283024c2d484190cd3fedf6a07cde95d)

## 幂平均

求数$x_1,x_2,\dots,x_n \gt 0 $的平均数。

我们依稀记得基本不等式：
$$
\frac{2} {\frac 1a +\frac 1 b}\le \sqrt {ab} \le \frac{a+b} 2  \le \sqrt{\frac {a^2 +b^2} 2}
$$

如果我们扩展到$n$个元，就有：
$$
\frac{n} {\sum {\frac 1 {x_i}}}\le (\prod{x_i})^{\frac 1 n}\le \frac{\sum x_i} {n} \le (\frac {(\sum x_i^{2})} n)^{\frac 1 2}
$$

注意到其实他们其中的三个都有形式：
$$
M(\alpha) = (\frac{\sum x_i^\alpha}{n})^{\frac 1\alpha}
$$
如果我们能让
$$
M(0) = \lim_{\alpha \to 0}  M(\alpha) = (\prod{x_i})^{\frac 1 n}
$$
的话，

基本不等式就可以写成
$$
M(-1) \le M(0) \le M(1) \le M(2)
$$
我们不禁猜想：$M(\alpha)$单调不减。
$$
\ln M(\alpha) = \frac 1 \alpha [\ln \sum x_i^\alpha - \ln n]
\\  (\ln M(\alpha))'=\frac{1}{\alpha}[\frac{ \sum x_i^\alpha\ln x_i}{\sum x_i^\alpha}] -\frac{1} {\alpha^2}[\ln \sum x_i^\alpha - \ln n]\\
=\frac n {\alpha^2\sum{x_i^\alpha}} (\frac{\sum x_i^{\alpha}\ln x_i^a}n - \frac{\sum x_i^\alpha }{n}\ln \frac{\sum x_i^\alpha}{n} )\\
=\frac n {\alpha^2\sum{x_i^\alpha}} (\frac{f(x_i^\alpha)}{n}-f(\frac{\sum x_i^\alpha}{n}))
$$
其中$f(x) = x\ln x$

由Jensen不等式，可以看出$(\ln M(\alpha))' \ge 0$，并且取等条件还是所有$x_i$相等，这符合我们的直觉。

那可以得出$M(\alpha)$分别在$(-\infty,0)$和$(0,\infty)$都单调不减。

再求$\lim_{\alpha \to 0} M(\alpha)$，由洛必达法则，
$$
\lim_{\alpha \to 0}\ln M(\alpha) = \lim_{\alpha \to 0}(\ln {\frac {\sum x_i^\alpha} n})/ \alpha
\\ =\lim_{\alpha \to 0}\frac{ \sum x_i^\alpha\ln x_i}{\sum x_i^\alpha} \\

=\frac {\sum\ln x_i} {n} = \ln(\  (x_1x_2\dots x_n)^\frac{1} n)
$$


真是十分美丽呢。



## MAX-MIN

 初学分析的时候常常遇到：
$$
\max (a,b) = \lim_{n\to +\infty} (a^n+b^n)^\frac 1 n
$$
这实际上就是求$\lim_{\alpha\to\infty} M(\alpha)$。

注意到对$\alpha > 0$，这是容易证明的，我们只需要像下面这样：

不妨假设$x_1 \le x_2 \le\dots \le x_n$
$$
x_n = (x_n^\alpha)^\frac 1 \alpha \le(\sum{x_i}^\alpha)^\frac 1 n \le (nx_n^\alpha)^\frac 1 n = n^\frac{1} \alpha x_n
$$
两边取极限夹逼即可。

同时$-\infty$的情况是一样的。

也就是说$M(-\infty) = \min(x) ,M(\infty) = max(x)$



那也就是说我们求比如$\max$的极值的时候，可以换成$M(\alpha;x)$在$\alpha \to \infty$时的极值。

是一个处理$\max$很好的工具。

## 椭圆函数

设
$$
0 \lt a_0 \le b_0  \\
a_n = M(0;\{a_{n-1},b_{n-1}\}),b_n = M(1;\{a_{n-1},b_{n-1}\})
$$
容易发现$a_{n-1} \le a_n \le b_n \le b_{n+1} $。

这代表$a_n$和$b_n$会收敛到同一个值$\phi(a,b)$

这个被称为算术几何平均。

可以证明
$$
\phi(a,b) = \int_0^{\frac \pi 2} \frac{d\theta} {\sqrt {a^2\cos^2\theta + b^2 \sin^2 \theta}}
$$
太难了，咕